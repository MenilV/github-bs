# OS / editor junk
.DS_Store

# Dependencies
node_modules/

# Build artifacts (the prod zip is assembled in CI)
*.zip

# Dev-only files (not part of the prod build)
preview.html

# Store-listing / promo assets (not part of the prod build)
github-bs.jpeg
screenshot1.png
screenshot2.png
